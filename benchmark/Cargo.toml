[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
bigtools = "0.5.6"
clap = { version = "4.5.60", features = ["derive"] }
getset = "0.1.6"
serde = { version = "1.0.228", features = ["derive"] }
