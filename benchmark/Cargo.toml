[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
bigtools = "0.5.8"
clap = { version = "4.6.1", features = ["derive"] }
getset = "0.1.7"
noodles = { version = "0.112.0", features = ["bed"] }
serde = { version = "1.0.228", features = ["derive"] }
